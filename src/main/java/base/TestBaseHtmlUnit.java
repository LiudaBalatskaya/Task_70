package base;

public class TestBaseHtmlUnit {
/*
    protected static final String BASE_URL = "https://192.168.100.26/";
    protected static final String BASE_URL1 = "http://1http://htmlunit.sourceforge.net/";
    public WebClient browser;

    public void loginPageChrome() throws Exception{

        try (final WebClient webClient = new WebClient()){
            webClient.getOptions().setJavaScriptEnabled(true);
            webClient.getOptions().setThrowExceptionOnScriptError(false);
            webClient.getOptions().setThrowExceptionOnFailingStatusCode(false);

            // I don't know at all why it doesn't work  final HtmlPage page = webClient.getPage(BASE_URL);
            final HtmlPage page = webClient.getPage(BASE_URL);
            webClient.waitForBackgroundJavaScript(10000);
            Assert.assertEquals("RMSys - Sign In",page.getTitleText());
        }
    }

    public void loginPageWebClient() throws Exception{
        try (final WebClient webClient = new WebClient()){
            // I don't know at all why it doesn't work  final HtmlPage page = webClient.getPage(BASE_URL);
            webClient.getPage(BASE_URL1);
            final HtmlPage page = webClient.getPage(BASE_URL);
            Assert.assertEquals("RMSys - Sign In",page.getTitleText());

            final String pageAsXml = page.asXml();
            AssertJUnit.assertTrue(pageAsXml.contains("<body class=\"topBarDisabled\">"));
        }
    }
    */
}
